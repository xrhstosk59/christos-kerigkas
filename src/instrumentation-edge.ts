// instrumentation-edge.ts
// Αυτό το αρχείο περιέχει κώδικα που θα εκτελεστεί μόνο στο Edge Runtime περιβάλλον

/**
 * Αρχικοποίηση του instrumentation για το Edge Runtime περιβάλλον
 */
export async function init() {
    // Εδώ μπορούμε να εκτελέσουμε κώδικα ειδικά για το Edge Runtime
    // Π.χ. βασικό logging, απλές μετρήσεις, κλπ.
    
    // Παράδειγμα: Εμφάνιση μηνύματος κατά την εκκίνηση του edge runtime
    if (process.env.NODE_ENV === 'development') {
      console.log('🌍 Edge Runtime instrumentation initialized in development mode');
    }
    
    // Εδώ θα μπορούσαμε να προσθέσουμε κώδικα για:
    // - Βασικές μετρήσεις Edge συμβατές
    // - Απλούς handlers για σφάλματα
    // - Βασικές μετρικές απόδοσης
    
    // Σημείωση: Το Edge Runtime έχει περιορισμούς σε σχέση με το Node.js,
    // οπότε πρέπει να είμαστε προσεκτικοί με τον κώδικα που εκτελούμε εδώ
  }
  
  // Άμεση εκτέλεση κατά την εισαγωγή
  init().catch(error => {
    console.error('❌ Error initializing Edge Runtime instrumentation:', error);
  });