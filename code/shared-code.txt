
=== C:\Users\chris\Downloads\christos-kerigkas\src\content\posts\crypto-trading-basics.json ===
{
    "slug": "crypto-trading-basics",
    "title": "Getting Started with Cryptocurrency Trading: A Developer's Perspective",
    "description": "Learn the fundamentals of crypto trading through a technical lens, including API integrations, data analysis, and automated trading strategies.",
    "date": "2024-01-25",
    "image": "/blog/crypto-trading.jpg",
    "author": {
      "name": "Christos Kerigkas",
      "image": "/profile.jpg"
    },
    "categories": ["Cryptocurrency", "Trading", "Programming", "Data Analysis"],
    "content": "## Introduction\n\nAs a developer venturing into cryptocurrency trading, understanding both technical analysis and programmatic approaches is crucial.\n\n## API Integration\n\n```python\nimport ccxt\n\ndef setup_exchange(exchange_id='binance'):\n    exchange_class = getattr(ccxt, exchange_id)\n    return exchange_class({\n        'apiKey': 'YOUR_API_KEY',\n        'secret': 'YOUR_SECRET',\n        'enableRateLimit': True\n    })\n```\n\n## Data Analysis\n\nUsing Python for market analysis:\n\n```python\nimport pandas as pd\n\ndef analyze_market_data(symbol, timeframe='1d'):\n    df = pd.DataFrame(exchange.fetch_ohlcv(symbol, timeframe))\n    df.columns = ['timestamp', 'open', 'high', 'low', 'close', 'volume']\n    return calculate_indicators(df)\n```\n\n## Risk Management\n\nImplementing stop-loss and take-profit:\n\n```python\ndef calculate_position_size(balance, risk_percentage, stop_loss):\n    risk_amount = balance * (risk_percentage / 100)\n    position_size = risk_amount / stop_loss\n    return position_size\n```\n\n## Building Trading Strategies\n\nDeveloping a simple moving average crossover strategy:\n\n```python\ndef sma_strategy(df, short_window=20, long_window=50):\n    df['SMA20'] = df['close'].rolling(window=short_window).mean()\n    df['SMA50'] = df['close'].rolling(window=long_window).mean()\n    \n    df['Signal'] = 0\n    df.loc[df['SMA20'] > df['SMA50'], 'Signal'] = 1\n    df.loc[df['SMA20'] < df['SMA50'], 'Signal'] = -1\n    \n    return df\n```\n\n## Next Steps\n\n1. Backtesting your strategies\n2. Implementing real-time monitoring\n3. Setting up automated alerts\n4. Developing risk management systems"
  }


=== C:\Users\chris\Downloads\christos-kerigkas\src\content\posts\getting-started-with-nextjs.json ===

{
    "slug": "getting-started-with-nextjs",
    "title": "Getting Started with Next.js and TypeScript",
    "description": "Learn how to build modern web applications using Next.js 13+ with TypeScript and best practices for performance and SEO.",
    "date": "2024-01-23",
    "image": "/blog/nextjs-typescript.jpg",
    "author": {
      "name": "Christos Kerigkas",
      "image": "/profile.jpg"
    },
    "categories": ["Next.js", "TypeScript", "Web Development"],
    "content": "## Introduction\n\nNext.js has become one of the most popular frameworks for building modern web applications...\n\n## Why Next.js?\n\nNext.js provides an excellent developer experience with features like...\n\n## Getting Started\n\nFirst, create a new Next.js project with TypeScript support..."
  }


=== C:\Users\chris\Downloads\christos-kerigkas\src\lib\image-loader.ts ===
// src/lib/image-loader.ts
interface ImageLoaderProps {
    src: string;
    width: number;
    quality?: number;
   }
   
   export default function imageLoader({ src, width, quality = 75 }: ImageLoaderProps): string {
    return `https://res.cloudinary.com/your-cloud/image/fetch/q_${quality},w_${width}/${src}`
   }


=== C:\Users\chris\Downloads\christos-kerigkas\src\lib\navigation.ts ===
// src/lib/navigation.ts
export function smoothScroll(e: React.MouseEvent<HTMLAnchorElement>, id: string) {
    e.preventDefault()
    const element = document.getElementById(id)
    if (element) {
      element.scrollIntoView({
        behavior: 'smooth',
        block: 'start',
      })
      // Update URL without reload
      window.history.pushState({}, '', `#${id}`)
    }
  }
  
  export function prefetchRoutes() {
    if (typeof window !== 'undefined') {
      // Prefetch main pages
      const links = ['/', '/blog', '/projects']
      links.forEach(href => {
        const link = document.createElement('link')
        link.rel = 'prefetch'
        link.href = href
        document.head.appendChild(link)
      })
    }
  }


=== C:\Users\chris\Downloads\christos-kerigkas\src\lib\seo.ts ===
import type { Metadata } from 'next'

export const siteConfig = {
  name: 'Christos Kerigkas',
  title: 'Christos Kerigkas | Full Stack Developer & Crypto Enthusiast',
  description: 'Full Stack Developer specializing in Next.js, React, and TypeScript. Building modern web applications and cryptocurrency trading solutions.',
  url: 'https://christoskerigkas.com',
  ogImage: '/og.png',
  links: {
    github: 'https://github.com/yourusername',
    linkedin: 'https://linkedin.com/in/yourusername',
    email: 'your-email@example.com'
  },
  author: {
    name: 'Christos Kerigkas',
    image: '/profile.jpg',
    bio: '21-year-old Full Stack Developer and Computer Science student based in Kavala, Greece.'
  }
}

export const defaultMetadata: Metadata = {
  title: siteConfig.title,
  description: siteConfig.description,
  openGraph: {
    type: 'website',
    url: siteConfig.url,
    title: siteConfig.title,
    description: siteConfig.description,
    siteName: siteConfig.name,
    images: [{
      url: siteConfig.ogImage,
      width: 1200,
      height: 630,
      alt: siteConfig.name
    }],
    locale: 'en_US',
  },
  twitter: {
    card: 'summary_large_image',
    title: siteConfig.title,
    description: siteConfig.description,
    images: [siteConfig.ogImage],
    creator: '@yourusername'
  },
  alternates: {
    canonical: siteConfig.url
  }
}


=== C:\Users\chris\Downloads\christos-kerigkas\src\lib\utils.ts ===
import { type ClassValue, clsx } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

export function formatDate(date: Date): string {
  return new Intl.DateTimeFormat('el-GR', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  }).format(date)
}

export function isExternalLink(url: string): boolean {
  return url.startsWith('http') || url.startsWith('mailto:') || url.startsWith('tel:')
}


=== C:\Users\chris\Downloads\christos-kerigkas\src\types\blog.ts ===
// src/types/blog.ts
export type BlogPost = {
    slug: string
    title: string
    description: string
    date: string
    image: string
    author: {
      name: string
      image: string
    }
    categories: string[]
    content: string
  }

